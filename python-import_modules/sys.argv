#!/usr/bin/python3
import sys
if __name__ == "__main__":
    total = 0
    for arg in sys.argv[1:]:  # sys.argv[0] proqramın adı olduğu üçün 1-dən başlamaq lazımdır
        total += int(arg)  # Hər arqumenti tam ədədə çeviririk və cəminə əlavə edirik
    print(total)  # Cəmi çap edir
